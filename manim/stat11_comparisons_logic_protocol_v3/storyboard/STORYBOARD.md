# Storyboard · Statistics 11 · Comparisons and Boolean Logic V3 SENIOR

1. Connect to previous lists class: `scores = [4.2, 2.8, 3.0, 4.5, 3.7]`.
2. Start from data cards and natural-language question.
3. Rebuild the simplified Colab execution model: code cell → Run → execution number → output.
4. Explicit `=` versus `==` split-screen logic.
5. Introduce six comparison operators one at a time, then assemble a 2×3 reference grid.
6. Show that comparison results are stored Boolean values and inspect `bool`.
7. Return to indexed list observations and compare `scores[3]` with `scores[1]`.
8. Motivate combined conditions from a valid-score range question.
9. AND: concrete cases first, then compact truth table and score examples.
10. OR: flagging rule with 2.8, 4.5 and 3.7, then compact truth table.
11. NOT: invert a Boolean answer with explicit False → NOT → True motion.
12. Consolidate AND / OR / NOT in three large cards.
13. Translate natural language to Python in four numbered steps.
14. Use parentheses only as readable grouping.
15. Mini workshop 1: three comparison predictions.
16. Mini workshop 2: AND / OR / NOT predictions.
17. Apply one valid-passing rule to each observation without introducing a loop.
18. Debug six common mistakes, including the idea that False is not an error.
19. Final challenge: `score = 4.7`, build both conditions, combine, predict, evaluate False.
20. Summary: comparison → Boolean → logic → data rule.
21. Bridge only: `if condition:` as the next lesson, without teaching conditionals.

## Repeated animation grammar

`DATA → QUESTION → EXPRESSION → PREDICT → RUN → OUTPUT → INTERPRET`

The lesson uses transformations, staged reveals, explicit waits and projection-safe typography instead of static slide dumps.
