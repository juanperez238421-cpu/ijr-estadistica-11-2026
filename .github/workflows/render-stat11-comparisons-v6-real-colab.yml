name: Render Statistics 11 Comparisons V6 REAL COLAB PQH

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: stat11-comparisons-v6-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  render:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    env:
      PROJECT_DIR: manim/stat11_comparisons_v6
      IMAGE: manimcommunity/manim:v0.20.1
      SCENE_FILE: src/stat11_comparisons_v6.py
      SCENE_CLASS: Stat11ComparisonsV6
      FINAL_NAME: Stat11_Comparisons_V6_REAL_COLAB_SENIOR_FINAL_PQH.mp4

    steps:
      - name: Checkout exact PR source
        uses: actions/checkout@v4

      - name: Reconstruct JP classroom style and source QA
        shell: bash
        run: |
          set -euo pipefail
          cd "$PROJECT_DIR"
          mkdir -p library delivery qa/audit_frames
          cat ../stat11_arrays_motivation/fragments/style_* > library/jp_classroom_style.py
          echo "3f3f06e94d5cad870ad335502cc1a93e56ce675abb1231ded5f9c71fd3e60e3d  library/jp_classroom_style.py" | sha256sum -c -
          sha256sum src/stat11_comparisons_v6.py | tee qa/source.sha256
          grep -q 'class Stat11ComparisonsV6' src/stat11_comparisons_v6.py
          grep -q 'def _play_triangle' src/stat11_comparisons_v6.py
          grep -q 'Polygon(' src/stat11_comparisons_v6.py
          grep -q 'RAM 0.8 / 12.7 GB' src/stat11_comparisons_v6.py
          grep -q 'Share' src/stat11_comparisons_v6.py

      - name: Pull pinned ManimCE 0.20.1 image
        run: docker pull "$IMAGE"

      - name: PQL smoke render and runtime lesson validation
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$PROJECT_DIR/media_pql"
          docker run --rm \
            --user "$(id -u):$(id -g)" \
            -e HOME=/tmp/manim-home \
            -v "$PWD:/manim" \
            -w "/manim/$PROJECT_DIR" \
            "$IMAGE" \
            python -m manim -ql "$SCENE_FILE" "$SCENE_CLASS" \
              --format=mp4 --media_dir media_pql --disable_caching
          PREVIEW="$(find "$PROJECT_DIR/media_pql" -type f -name '*.mp4' ! -path '*/partial_movie_files/*' | head -n 1)"
          test -n "$PREVIEW" && test -s "$PREVIEW"

      - name: PQH final render
        shell: bash
        run: |
          set -euo pipefail
          rm -rf "$PROJECT_DIR/media_final"
          docker run --rm \
            --user "$(id -u):$(id -g)" \
            -e HOME=/tmp/manim-home \
            -v "$PWD:/manim" \
            -w "/manim/$PROJECT_DIR" \
            "$IMAGE" \
            python -m manim -qh "$SCENE_FILE" "$SCENE_CLASS" \
              --fps 30 --format=mp4 --media_dir media_final --disable_caching
          FINAL="$(find "$PROJECT_DIR/media_final" -type f -name '*.mp4' ! -path '*/partial_movie_files/*' | head -n 1)"
          test -n "$FINAL" && test -s "$FINAL"
          cp "$FINAL" "$PROJECT_DIR/delivery/$FINAL_NAME"
          cp "$PROJECT_DIR/src/stat11_comparisons_v6.py" "$PROJECT_DIR/delivery/stat11_comparisons_v6.py"

      - name: Technical and dense visual QA
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          V="$PROJECT_DIR/delivery/$FINAL_NAME"
          ffprobe -v error \
            -show_entries stream=codec_name,width,height,r_frame_rate,pix_fmt \
            -show_entries format=duration,size \
            -of default=nw=1 "$V" | tee "$PROJECT_DIR/qa/ffprobe.txt"
          grep -q '^codec_name=h264$' "$PROJECT_DIR/qa/ffprobe.txt"
          grep -q '^width=1920$' "$PROJECT_DIR/qa/ffprobe.txt"
          grep -q '^height=1080$' "$PROJECT_DIR/qa/ffprobe.txt"
          grep -q '^pix_fmt=yuv420p$' "$PROJECT_DIR/qa/ffprobe.txt"
          grep -q '^r_frame_rate=30/1$' "$PROJECT_DIR/qa/ffprobe.txt"
          ffmpeg -nostdin -v error -i "$V" -f null - 2> "$PROJECT_DIR/qa/decode_errors.log"
          test ! -s "$PROJECT_DIR/qa/decode_errors.log"
          sha256sum "$V" | tee "$PROJECT_DIR/qa/final_mp4.sha256"
          ffmpeg -nostdin -v error -i "$V" \
            -vf "fps=1/10,scale=960:-2" \
            -frames:v 36 "$PROJECT_DIR/qa/audit_frames/frame_%02d.jpg"
          test "$(find "$PROJECT_DIR/qa/audit_frames" -type f -name '*.jpg' | wc -l)" -ge 18
          ffmpeg -nostdin -v error -i "$V" \
            -vf "fps=1/10,scale=384:-2,tile=5x6:padding=4:margin=4" \
            -frames:v 1 "$PROJECT_DIR/qa/contact_sheet.jpg"
          test -s "$PROJECT_DIR/qa/contact_sheet.jpg"

      - name: Upload final V6 MP4
        uses: actions/upload-artifact@v4
        with:
          name: Stat11-Comparisons-V6-REAL-COLAB-SENIOR-FINAL-PQH
          retention-days: 30
          if-no-files-found: error
          path: manim/stat11_comparisons_v6/delivery/Stat11_Comparisons_V6_REAL_COLAB_SENIOR_FINAL_PQH.mp4

      - name: Upload V6 source and QA
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: Stat11-Comparisons-V6-REAL-COLAB-QA
          retention-days: 30
          if-no-files-found: warn
          path: |
            manim/stat11_comparisons_v6/delivery/stat11_comparisons_v6.py
            manim/stat11_comparisons_v6/qa/source.sha256
            manim/stat11_comparisons_v6/qa/ffprobe.txt
            manim/stat11_comparisons_v6/qa/decode_errors.log
            manim/stat11_comparisons_v6/qa/final_mp4.sha256
            manim/stat11_comparisons_v6/qa/contact_sheet.jpg
            manim/stat11_comparisons_v6/qa/audit_frames/*.jpg
